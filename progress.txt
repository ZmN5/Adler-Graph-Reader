## Codebase Patterns
- Use `&*pool` when passing `State<SqlitePool>` from Axum extractors to sqlx query executors (State wraps the pool, needs deref to get `&Pool` which implements Executor)
- When using Axum Router with `.with_state(pool)`, the pool is moved into the router state and accessed via `axum::extract::State<T>`
- Rust: Import routing::{get, put} for both GET and PUT route handlers
- In Axum handlers with multiple extractors, State must come before other extractors (e.g., State<SqlitePool> first, then Multipart)
- Use custom AppError enum implementing IntoResponse for consistent error handling across handlers

## 2026-03-28 - US-003
- What was implemented
  - Implemented GET /api/settings/language returning {"language": "zh"} or {"language": "en"}
  - Implemented PUT /api/settings/language accepting {"language": "zh"} or {"language": "en"}
  - Default language is zh (Chinese) as set by db.rs init_database
  - Settings stored in SQLite settings table

- Files changed
  - backend/src/main.rs - added settings handlers and routes

- **Learnings for future iterations:**
  - Axum State extractors need `&*pool` syntax for sqlx queries, not just `&pool`
  - When using `.with_state(pool)` in Router, drop(pool) at end is not needed
  - Need to import `routing::put` when adding PUT endpoints alongside GET

## 2026-03-28 - US-004
- What was implemented
  - Implemented POST /api/books/upload accepting multipart/form-data with file, title, author fields
  - Implemented GET /api/books returning list of books (id, title, author, format, total_pages)
  - Implemented GET /api/books/:id returning full book details including file_path
  - Implemented DELETE /api/books/:id deleting book and cascade deleting chunks, nodes, edges
  - Files saved to ./data/books/{book_id}.{format}
  - Added AppError enum with proper IntoResponse implementation for error handling

- Files changed
  - backend/src/main.rs - added books handlers, routes, and AppError type
  - backend/Cargo.toml - enabled axum multipart feature

- **Learnings for future iterations:**
  - When using Multiple extractors in Axum (State, Multipart), State must come first in parameter order
  - Use custom AppError enum implementing IntoResponse for consistent error handling across handlers
  - MultipartError and sqlx errors need explicit conversion to AppError with .map_err(|e| e.to_string())
  - Return type of (StatusCode, Json<T>) for responses with explicit status codes

## 2026-03-28 - US-005
- What was implemented
  - Implemented GET /api/books/:id/chunks returning array of chunks
  - Each chunk has: id, book_id, page_start, page_end, content (first 200 chars)
  - Implemented GET /api/chunks/:id returning full chunk content
  - Added ChunkSummary and ChunkDetails types

- Files changed
  - backend/src/main.rs - added chunk handlers and routes

- **Learnings for future iterations:**
  - For truncated content display, use content.chars().take(200).collect::<String>() to handle Unicode properly

## 2026-03-28 - US-006
- What was implemented
  - Implemented GET /api/books/:id/graph returning {nodes: [...], edges: [...]} for a book
  - Implemented GET /api/graph/global returning {nodes: [...], edges: [...]} for all books
  - Implemented GET /api/nodes/:id returning node details with full description, examples, source_chunk_ids
  - GraphNode: id, name, description, examples (array), source_chunk_ids (array)
  - GraphEdge: id, source_node_id, target_node_id, relation_type

- Files changed
  - backend/src/main.rs - added graph handlers and routes

- **Learnings for future iterations:**
  - When querying edges for a book, need to filter by both source AND target node belonging to the book
  - Use serde_json::from_str for parsing JSON arrays from database (examples, source_chunk_ids columns)
  - sqlx query_as tuple must match exact column count in SELECT statement

## 2026-03-28 - US-007
- What was implemented
  - Implemented PDF parsing using pdf-extract crate
  - Implemented POST /api/books/:id/parse endpoint
  - Extract text per page maintaining page numbers
  - Split pages into ~1000 char chunks with 100 char overlap
  - Store chunks in database with UUIDs and page_start/page_end
  - Update total_pages in books table after parsing
  - Ignore images, extract only text

- Files changed
  - backend/Cargo.toml - added pdf-extract dependency
  - backend/src/pdf_parser.rs - new file with PDF parsing logic
  - backend/src/main.rs - added parse endpoint and route

- **Learnings for future iterations:**
  - pdf-extract extracts text from PDF files, may not preserve exact page boundaries
  - Use split_pages_from_text() to detect page breaks via form feeds or "Page X" patterns
  - When format column in query is TEXT, use String type, not i32

## 2026-03-28 - US-008
- What was implemented
  - Implemented EPUB parsing using zip crate (EPUB is a ZIP archive)
  - Extract text from XHTML/HTML content files in the EPUB
  - Chapters treated as pages (total_pages = number of chapters)
  - Split chapters into ~1000 char chunks with 100 char overlap
  - Store chunks in database with UUIDs and chapter index as page_start/page_end
  - Ignore images, extract only text

- Files changed
  - backend/Cargo.toml - added zip crate dependency
  - backend/src/epub_parser.rs - new file with EPUB parsing logic
  - backend/src/main.rs - updated parse endpoint to handle both PDF and EPUB

- **Learnings for future iterations:**
  - EPUB files are ZIP archives - use zip crate to extract contents
  - Content files are XHTML/HTML - need to strip tags to get plain text
  - Remove <script> and <style> tags before stripping HTML tags
  - Chapters are treated as "pages" for EPUB (total_pages = chapter count)

## 2026-03-28 - US-009
- What was implemented
  - Created LLM client connecting to http://localhost:1234/v1 (LM Studio)
  - OpenAI-compatible chat completions API support
  - Built system prompt enforcing output language (zh/en) and source_chunk_id
  - Parse JSON response extracting concepts, relations, explanations, examples
  - Handle connection errors gracefully with custom LlmError enum
  - Added reqwest dependency for HTTP client

- Files changed
  - backend/Cargo.toml - added reqwest dependency
  - backend/src/llm_client.rs - new file with LLM client implementation
  - backend/src/main.rs - added llm_client module

- **Learnings for future iterations:**
  - Use serde aliases (e.g., #[serde(alias = "source_chunk_id")]) for JSON fields with different names
  - LM Studio uses OpenAI-compatible API at /v1/chat/completions
  - Build language-specific prompts for concept extraction

## 2026-03-28 - US-010
- What was implemented
  - Implemented POST /api/books/:id/extract endpoint
  - Process chunks concurrently with max 3 parallel requests using Semaphore
  - For each chunk, call LLM extract_concepts and create nodes/edges
  - Nodes created for each extracted concept
  - Edges created for each extracted relation (auto-creates nodes if not found)
  - Return {status: "completed", nodes_count, edges_count}
  - Added basic entity matching (case-insensitive substring match for nodes)

- Files changed
  - backend/src/extractor.rs - new file with extraction pipeline
  - backend/src/main.rs - added extract endpoint and route

- **Learnings for future iterations:**
  - Use Arc<Semaphore> with acquire().await to limit concurrent async operations
  - sqlx::query_scalar::<_, T> requires explicit type annotation for the output type
  - tokio::spawn takes ownership, so clone pool/llm inside the loop before spawning
  - sqlx::query returns (), not a result row, so use execute() not fetch_one()


## 2026-03-28 - US-011
- What was implemented
  - Entity deduplication in concept extraction pipeline
  - create_node_for_concept now checks for existing similar nodes before creating new ones
  - Uses case-insensitive substring matching via existing find_node_by_name function
  - When similar node found, merges source_chunk_id and examples into existing node
  - create_edge_for_relation now merges into existing nodes when found
  - Added duplicate edge detection (checks source_node_id, target_node_id, relation_type)
  - When edge already exists, merges source_chunk_id into existing edge
  - Added merge_node_sources and merge_edge_sources helper functions

- Files changed
  - backend/src/extractor.rs - added deduplication logic and merge helper functions

- **Learnings for future iterations:**
  - ExtractedRelation has 'explanation' field, not 'description' or 'examples'
  - Relations don't have examples, so when merging via relation use empty vec
  - Use existing find_node_by_name function for deduplication checking
  - Edge deduplication checks (source_node_id, target_node_id, relation_type) tuple

## 2026-03-28 - US-012
- What was implemented
  - Created Vite + React + TypeScript frontend project in /frontend
  - Configured Tailwind CSS with shadcn/ui theme tokens
  - Set up Zustand store for app state (loading/error states)
  - Created Header and MainContent layout components
  - Built API client with typed methods for localhost:8080
  - Configured Vite proxy for /api to localhost:8080

- Files changed
  - frontend/ - new directory with complete React project structure
  - frontend/package.json, vite.config.ts, tsconfig.json, tailwind.config.js, postcss.config.js
  - frontend/src/main.tsx, App.tsx, index.css
  - frontend/src/components/Header.tsx, MainContent.tsx
  - frontend/src/stores/app-store.ts
  - frontend/src/lib/utils.ts, api-client.ts

- **Learnings for future iterations:**
  - Use Vite path aliases (@/*) for clean imports
  - shadcn/ui uses CSS variables for theming - define in index.css
  - API client should be centralized in lib/api-client.ts for all components
  - Zustand store should only hold UI state, not server data (use React Query or SWR for server data)
