## Codebase Patterns
- Use `cn()` utility from `@/lib/utils` for merging Tailwind classes
- Modal/dialog components should be placed at the end of the return JSX, inside the root element
- Use `useCallback` for event handlers that are passed to child components to avoid unnecessary re-renders
- Book list state uses `BookSummary` type from `@/lib/api-client`

## 2026-03-29 20:45 - US-001
- What was implemented
  - Created `ConfirmDialog.tsx` component with backdrop, close on Escape key, close on backdrop click, danger variant styling
  - Modified `BookList.tsx` to use state-managed modal instead of browser `confirm()` dialog
  - Delete button now opens modal showing "Confirm delete '{book name}'? This cannot be undone."
  - Cancel button closes modal without action, Confirm button executes deletion
- Files changed
  - frontend/src/components/BookList.tsx (modified)
  - frontend/src/components/ConfirmDialog.tsx (new)
  - prd.json (updated US-001 passes to true)
- **Learnings for future iterations:**
  - When adding modal components to a list view, ensure the modal is placed inside the root JSX element but after the `.map()` iteration
  - Modal state needs both `isOpen` boolean and the item being acted upon (e.g., `bookToDelete`)
  - The `confirm()` browser dialog should never be used - always create a proper modal component
  - For delete actions, use `variant="danger"` to show appropriate warning styling
---
