## Concept Extraction Enhancement - Ralph Progress

Branch: ralph/concept-extraction-enhancement
Started: 2026-03-29

---

## 2026-03-29 - US-006
- Updated extraction flow to read book language from books table instead of global settings
- Language mapping: 'auto' → 'zh', 'zh' → 'zh', 'en' → 'en' for backward compatibility
- Integrated core concept identification to run automatically after extraction completes
- Updated ExtractResponse to include core_concepts_count field
- Extraction API now returns core concept count in the response
- Graceful handling: core concept identification failures don't fail the entire extraction
- Files changed: backend/src/extractor.rs, backend/src/main.rs
- **Learnings for future iterations:**
  - Extraction uses book-specific language config instead of global setting
  - Core concept identification runs as post-processing step after extraction
  - API response structure changes should maintain backward compatibility
  - Pool reference handling: use &pool when calling async functions expecting &SqlitePool
  - cargo check and all tests pass
---
- Added language selection UI to book upload form with options: Auto, Chinese (中文), English
- Updated backend upload_book API to accept and store language parameter
- Modified apiUploadBook function to support language parameter with default 'auto'
- Implemented two-step upload flow: file selection → language configuration → upload
- Files changed: backend/src/main.rs, frontend/src/lib/api-client.ts, frontend/src/components/UploadButton.tsx
- **Learnings for future iterations:**
  - Multipart form fields are processed sequentially in axum's Multipart extractor
  - Backend already has language column in books table with 'auto' default
  - Two-step UI pattern (select file → configure → upload) provides better UX for optional parameters
  - Both cargo check and tsc pass with all changes
---


- Chunk size constants are defined directly in pdf_parser.rs and epub_parser.rs (not centralized)
- Approximate token-to-char ratio: ~4 characters per token (16000 chars ≈ 4000 tokens)
- Overlap ratio: 10% of chunk size (400 char overlap for 4000 char chunks)
- No sampling logic exists - all content is already processed
- LLM prompts include JSON Schema for structured output validation
- Validation logic filters out invalid concepts/relations with detailed logging
- **Core Concept Identification:** Uses PageRank + frequency scoring with 50/50 weight
  - PageRank damping factor: 0.85, iterations: 20
  - Default: top 20% of nodes (min 5, max 20) marked as core
  - API endpoint: POST /api/books/{id}/identify-core-concepts to trigger
  - API endpoint: GET /api/books/{id}/core-concepts to retrieve list
  - GraphNode includes is_core field for visualization

---

## 2026-03-29 - US-002
- Modified chunk size from 1000 to 16000 characters (~4000 tokens) in both parsers
- Updated overlap from 100 to 400 characters (maintaining 10% ratio)
- Updated comments to document new chunking strategy
- Verified no sampling logic exists (all content already processed)
- Files changed: backend/src/pdf_parser.rs, backend/src/epub_parser.rs
- **Learnings for future iterations:**
  - Chunking parameters are hardcoded in each parser separately (no shared config)
  - 16000 characters ≈ 4000 tokens (using 4 chars/token approximation)
  - cargo check passes with all changes
---

## 2026-03-29 - US-003
- Enhanced LLM extraction prompt with detailed output requirements:
  * System prompt now requires 2-3 sentence descriptions
  * System prompt specifies 1-2 examples per concept
  * System prompt includes page_number requirement with extraction instructions
- Added JSON Schema definition to user prompt for structured output validation
- Enhanced error handling in parse_extraction_response:
  * Added detailed validation for concepts (name, description, examples, page_number)
  * Added detailed validation for relations (source/target names, relation_type)
  * Graceful handling of malformed responses with detailed logging
  * Invalid items are skipped instead of failing entire extraction
- Files changed: backend/src/llm_client.rs
- **Learnings for future iterations:**
  - JSON Schema in prompts helps LLM produce valid structured output
  - Validation logic should filter invalid items rather than fail entire batch
  - page_number validation filters non-positive values (p > 0)
  - Examples are filtered to remove empty strings
  - cargo check passes with all changes
---

## 2026-03-29 - US-004
- Implemented core concept identification algorithm in `core_concept.rs`:
  * PageRank algorithm: 0.85 damping factor, 20 iterations for centrality scoring
  * Frequency scoring: counts how many chunks reference each concept
  * Composite score: 50% frequency + 50% PageRank
  * Top 20% (min 5, max 20) of concepts marked as core by default
- Added API endpoints:
  * POST /api/books/{id}/identify-core-concepts - triggers identification algorithm
  * GET /api/books/{id}/core-concepts - retrieves list of core concepts
- Updated GraphNode struct to include `is_core: bool` field
- Updated get_book_graph and get_global_graph to return is_core field
- Files changed: backend/src/core_concept.rs (new), backend/src/main.rs
- **Learnings for future iterations:**
  - PageRank implementation follows standard power iteration with damping
  - Composite scoring allows easy weight adjustment if needed
  - Core concept identification should run after extraction is complete
  - Tests added for PageRank edge cases (empty, single node, cycle)
  - cargo check and all tests pass
---


## 2026-03-29 - US-007
- Added is_core field to GraphNode type in api-client.ts
- Updated ExtendedNode interfaces in both GraphCanvas and GlobalGraphView to include is_core property
- Modified node rendering to visually distinguish core concepts:
  * Core concepts are 1.5x larger than regular concepts
  * Core concepts use purple color (#8b5cf6) with violet border (#7c3aed)
  * Core concept labels are bold
  * Selected/hovered state takes precedence over core concept styling
- Added legend component to both GraphCanvas and GlobalGraphView:
  * Shows core concept count and regular concept count
  * Positioned at top-right with backdrop blur effect
  * Styled consistently with the app design system
- Updated GlobalGraphView node rendering with same core concept styling
- Files changed: frontend/src/lib/api-client.ts, frontend/src/components/GraphCanvas.tsx, frontend/src/components/GlobalGraphView.tsx
- **Learnings for future iterations:**
  - ExtendedNode interface must match GraphNode from api-client (including is_core)
  - Canvas node rendering: node size can be adjusted based on is_core flag
  - Force-graph canvas rendering uses globalScale for responsive sizing
  - Legend positioning uses absolute positioning with backdrop-blur for modern glassmorphism effect
  - Both graph views (book-specific and global) should have consistent styling
  - TypeScript: when adding new fields to shared types, all consumers must be updated
  - tsc passes with all changes
---


## 2026-03-29 - US-008
- Implemented node detail panel with three-column layout (PDF | Graph | Detail Panel)
- Updated backend `/api/nodes/{id}` endpoint to include page_number field
- Updated frontend NodeDetails type to include page_number
- Added "View in PDF" button that jumps to the concept's page number
- Modified PDFReader to accept pageNumber prop for external navigation
- NodeDetailPanel fetches full node details including examples and page_number
- Three-column layout is responsive - right panel collapses when no node selected
- Files changed: backend/src/main.rs, frontend/src/components/NodeDetailPanel.tsx, frontend/src/components/PDFReader.tsx, frontend/src/components/ThreeColumnLayout.tsx (new), frontend/src/App.tsx, frontend/src/lib/api-client.ts
- **Learnings for future iterations:**
  - When adding new fields to backend API, update both the SQL query and the response struct
  - PDFReader can be controlled externally via pageNumber prop - triggers navigation via useEffect
  - ThreeColumnLayout pattern: left/center panels flex, right panel fixed width (320px), collapsible
  - NodeDetailPanel uses existing getNode API to fetch full details (examples, page_number) beyond what's in GraphNode
  - Both cargo check and tsc pass with all changes
---

## 2026-03-29 - US-009
- PDF page jump functionality was primarily implemented in US-008 with three-column layout
- Enhanced EPUBReader to support pageNumber and totalPages props for approximate page navigation
- EPUB uses percentage-based navigation since EPUB doesn't have fixed page numbers
- Navigation formula: percentage = (pageNumber - 1) / totalPages
- Three-column layout ensures users always see graph (center) while viewing document (left)
- Files changed: frontend/src/components/EPUBReader.tsx, frontend/src/App.tsx
- **Learnings for future iterations:**
  - PDF page jump: PDFReader uses pageNumber prop with useEffect to trigger navigation via goToPage
  - EPUB page approximation: Uses rendition.display(percentage) for location-based navigation
  - Three-column layout provides seamless PDF-to-graph workflow without explicit "return" action needed
  - Both cargo check and tsc pass with all changes
---

## 2026-03-29 - US-010
- Added CoreConceptsList component to display core concepts in a dedicated view
- Added tab navigation in book detail view: 'Concept Graph' and 'Core Concepts'
- CoreConceptsList fetches core concepts via /api/books/{id}/core-concepts endpoint
- Each concept card shows: name, description (2 lines), examples, page number
- 'View' button on each concept jumps to PDF page (if page_number available)
- Clicking concept opens it in graph view (switches to graph tab and selects node)
- Handles empty state with helpful message when no concepts extracted
- Handles loading state with skeleton/spinner
- Handles error state with error message
- Files changed: frontend/src/components/CoreConceptsList.tsx (new), frontend/src/App.tsx, frontend/src/lib/api-client.ts
- **Learnings for future iterations:**
  - Tab navigation pattern: use state for active tab, styled buttons with active indicator
  - CoreConceptsList loads full node details to get page_number (not in GraphNode)
  - Pattern: list item click handler can switch tabs by calling setActiveTab from parent
  - Examples display limited to 2 with '+N more' indicator to save space
  - Both cargo check and tsc pass with all changes
---

