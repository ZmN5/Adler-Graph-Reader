# Ralph Progress Log
Started: Sat Mar 28 16:08:53 CST 2026
---

## 2026-03-28 16:20 - US-001
- Implemented Rust backend project with Axum framework
- Created Cargo.toml with dependencies: axum, tokio, serde, serde_json, sqlx, uuid, chrono, tower, tower-http, tracing, tracing-subscriber
- Created basic Axum server with CORS enabled
- Added GET /api/health endpoint returning {"status":"ok"}
- Server starts on localhost:8080
- Typecheck and build pass
- Files changed: backend/Cargo.toml, backend/src/main.rs
- **Learnings for future iterations:**
  - Axum 0.8 requires explicit Json wrapper for handler return types
  - Use tower-http CorsLayer with Any for allowing all origins during development
  - edition = "2021" is the stable Rust edition, not "2024"
  - cargo check is faster for typechecking, cargo build for full compilation
---

## 2026-03-28 16:35 - US-002
- Implemented SQLite database schema with books, chunks, nodes, edges, and settings tables
- Created db.rs module with create_pool, init_database, and ensure_data_dir functions
- Updated main.rs to initialize database on startup
- Created ./data directory automatically if not exists
- Database URL: sqlite://./data/reader.db
- Default language 'zh' inserted on first run
- Typecheck and build pass
- Files changed: backend/src/db.rs, backend/src/main.rs
- **Learnings for future iterations:**
  - sqlx 0.8 uses sqlite:// prefix (not sqlite3://)
  - Use IF NOT EXISTS for all CREATE TABLE statements to allow safe re-initialization
  - Foreign key constraints ON DELETE CASCADE for proper cleanup
  - JSON columns store serialized arrays as TEXT in SQLite
---

## 2026-03-28 - EPUBReader Fix
- Fixed EPUBReader component to use Blob URL approach with 30-second timeout
- Simplified error handling and cleanup of Blob URLs
- Marked US-001 through US-005 as passes: true in PRD (all were committed previously but PRD was not updated)
- Files changed: frontend/src/components/EPUBReader.tsx, tasks/prd.json
- **Learnings for future iterations:**
  - EPUB loading issues may be due to epubjs internal resource fetching - Blob URL approach helps
  - The PRD at tasks/prd.json is separate from the original prd.json at project root
  - All US-001 through US-005 were committed in previous sessions but PRD was never updated
  - The `start.sh` at project root is actually `scripts/run-evaluation.sh` being called by `start.sh`


## 2026-03-28 22:20 - US-006 US-007
- Implemented before/after comparison in run-evaluation.sh report generation
- Created run-evaluation-loop.sh for iteration tracking
- Added Section 四 (修复前后对比) to evaluation reports showing issue status changes
- Iteration loop tracks iterations in evaluation-iterations.log
- Loop stops when no BLOCKING issues remain or max iterations (10) reached
- Updated tasks/prd.json to mark all stories as passes: true
- Typecheck passes for both frontend and backend
- Files changed: scripts/run-evaluation.sh, scripts/run-evaluation-loop.sh, tasks/prd.json
- **Learnings for future iterations:**
  - The PRD at tasks/prd.json is the working PRD, not the deleted prd.json at root
  - run-evaluation.sh generates reports with before/after comparison automatically
  - run-evaluation-loop.sh is a wrapper that runs evaluation multiple times and tracks BLOCKING issues
  - Iteration log is at tasks/evaluation-iterations.log
  - The start.sh at project root is a wrapper that calls scripts/run-evaluation.sh
  - When editing bash scripts with heredocs, watch for typos in section headers (e.g., "vscode4.1" instead of "4.1")

---

## 2026-03-28 - US-001 (5-round evaluation loop)
- Ran first evaluation round using project-evaluator agent
- Backend health check passes (GET /api/health returns {"status":"ok"})
- Core functionality tested: upload, parse, file serving, PDF reading, graph infrastructure
- Generated evaluation report with problem classification (BLOCKING/HIGH/MEDIUM/SUGGESTION)
- Report saved to tasks/evaluation-report-2026-03-28.md
- BLOCKING issue found: Data directory path mismatch (backend stores at backend/data/ instead of project root data/)
- HIGH issue: LM Studio model not auto-loaded (external dependency)
- HIGH issue: Extraction lacks graceful error handling
- MEDIUM issues: Test Book PDF corrupted, Port configuration documentation
- Typecheck passes for both backend (cargo check) and frontend (tsc --noEmit)
- Files changed: tasks/evaluation-report-2026-03-28.md (new), tasks/prd.json (updated)
- Committed as: feat: US-001 - 第1轮：首次评估
- **Learnings for future iterations:**
  - Working tree PRD (tasks/prd.json) may differ from HEAD - the 5-round loop PRD was created but not committed in previous session
  - The staged version (index) had old 7-story PRD with passes: true, but working tree had new 11-story PRD with passes: false
  - Start.sh may fail with "Address already in use" if backend is already running - this is not an error
  - Frontend may use port 3001 if 3000 is in use
  - project-evaluator agent can run comprehensive evaluation including browser traversal
  - Evaluation report file must be manually staged with git add before commit

## 2026-03-28 22:50 - US-002
- Fixed BLOCKING issue: Data directory path mismatch
- Changed start.sh to run `cargo run -p backend` from project root instead of `cd backend && cargo run`
- This ensures std::env::current_dir() returns project root, so data is stored at {project_root}/data/ instead of {project_root}/backend/data/
- Backend typecheck passes (cargo check)
- Frontend typecheck passes (tsc --noEmit)
- Files changed: start.sh, tasks/prd.json
- **Learnings for future iterations:**
  - start.sh runs `cd "$SCRIPT_DIR/backend"` before cargo run, causing backend to use backend/ as current dir
  - The fix uses `cargo run -p backend` from project root, which Cargo interprets as running the backend package
  - CLAUDE.md specifies "后端必须从项目根目录启动以确保路径正确" - backend must start from project root
